---
import BaseLayout from './BaseLayout.astro';

type Props = {
  title: string;
  description?: string;
  variant: string;
};

const { title, description, variant } = Astro.props;
---

<BaseLayout title={title} description={description} variantMode variant={variant}>
  <slot />
</BaseLayout>
